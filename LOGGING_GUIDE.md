# Руководство по просмотру логов iOS приложения

> ⚡ **Быстрый старт:** В DEBUG режиме логи автоматически выводятся в терминал Cursor/SweetPad! Просто запустите приложение и смотрите логи в реальном времени.

## Система логирования

iOS приложение использует нативную систему **OSLog** (через `Logger` API) для логирования. Все логи централизованы через `AppLogger` класс.

**Subsystem:** `riqtu.Hohma`  
**Файл:** `Hohma/Core/Services/AppLogger.swift`

**Особенность:** В DEBUG режиме логи дублируются в терминал (stdout/stderr) для удобного просмотра в Cursor/SweetPad, а также сохраняются в системное логирование OSLog.

## Категории логов

Приложение использует следующие категории для организации логов:

- **Network** - сетевые запросы и ответы
- **Authentication** - авторизация и аутентификация
- **SocketIO** - WebSocket соединения
- **UI** - пользовательский интерфейс
- **Cache** - кэширование данных
- **Keychain** - работа с Keychain
- **General** - общие логи (по умолчанию)

## Уровни логирования

- **🔍 debug** - отладочная информация (только в DEBUG режиме)
- **ℹ️ info** - информационные сообщения
- **⚠️ warning** - предупреждения
- **❌ error** - ошибки
- **💥 fault** - критические ошибки

## Где смотреть логи

### 1. В терминале (Cursor/SweetPad) ⭐ РЕКОМЕНДУЕТСЯ

**Логи автоматически выводятся в терминал в DEBUG режиме!**

При запуске приложения через Cursor или SweetPad, все логи будут видны прямо в терминале:

```bash
# Просто запустите приложение, логи появятся автоматически
# Формат вывода:
[2025-12-12 10:30:45.123] ℹ️ [Network] NetworkManager.swift:42 request() - Request started
[2025-12-12 10:30:46.456] ❌ [Authentication] AuthService.swift:72 login() - Login failed | Error: Invalid credentials
```

**Особенности:**

- ✅ Логи выводятся в реальном времени
- ✅ Ошибки идут в stderr (красным в большинстве терминалов)
- ✅ Остальные логи идут в stdout
- ✅ Автоматически включено в DEBUG режиме
- ✅ Отключено в Release (для безопасности)

**Управление выводом в консоль:**

```swift
// Отключить вывод в терминал (если нужно)
AppLogger.shared.enableConsoleOutput = false

// Включить обратно
AppLogger.shared.enableConsoleOutput = true
```

### 2. В Xcode Console (во время разработки)

**Альтернативный способ** - логи отображаются автоматически в консоли Xcode:

1. Запустите приложение в Xcode (⌘R)
2. Откройте консоль (⌘⇧Y или View → Debug Area → Activate Console)
3. Логи будут отображаться в реальном времени

**Фильтрация в Xcode:**

- Используйте поиск в консоли для фильтрации по категориям или тексту
- Например: `category:Network` или `category:Authentication`

### 2. Через Console.app (macOS)

**Приложение Console** позволяет просматривать логи с подключенных устройств:

1. Откройте **Console.app** (находится в `/Applications/Utilities/`)
2. В левой панели выберите ваше устройство или симулятор
3. В поиске введите: `subsystem:riqtu.Hohma`
4. Для фильтрации по категории добавьте: `category:Network` или `category:Authentication`

**Полезные фильтры:**

```
subsystem:riqtu.Hohma
subsystem:riqtu.Hohma category:Network
subsystem:riqtu.Hohma category:Authentication
subsystem:riqtu.Hohma level:error
subsystem:riqtu.Hohma level:fault
```

### 3. Через терминал (log stream) - для системных логов OSLog

**Если нужно просмотреть системные логи OSLog (в дополнение к выводу в терминал):**

```bash
# Все логи приложения (от OSLog)
log stream --predicate 'subsystem == "riqtu.Hohma"'

# Только ошибки
log stream --predicate 'subsystem == "riqtu.Hohma" AND (level == "error" OR level == "fault")'

# Логи конкретной категории
log stream --predicate 'subsystem == "riqtu.Hohma" AND category == "Network"'

# Логи с подключенного устройства (замените UDID на ID вашего устройства)
log stream --device <UDID> --predicate 'subsystem == "riqtu.Hohma"'
```

**Примечание:** В DEBUG режиме логи уже выводятся в терминал автоматически, поэтому `log stream` обычно не нужен для разработки.

**Просмотр сохраненных логов:**

```bash
# Последние логи приложения
log show --predicate 'subsystem == "riqtu.Hohma"' --last 5m

# Логи за последний час
log show --predicate 'subsystem == "riqtu.Hohma"' --last 1h

# Логи за конкретный период
log show --predicate 'subsystem == "riqtu.Hohma"' --start "2025-12-12 10:00:00" --end "2025-12-12 11:00:00"

# Экспорт логов в файл
log show --predicate 'subsystem == "riqtu.Hohma"' --last 1h > ~/Desktop/hohma_logs.txt
```

### 4. Через Instruments

Для детального анализа производительности и логирования:

1. Откройте Xcode → Product → Profile (⌘I)
2. Выберите инструмент **System Trace** или **Logging**
3. Запустите запись и воспроизведите проблему
4. Остановите запись и проанализируйте логи

## Примеры использования

### Просмотр сетевых запросов

```bash
# В терминале
log stream --predicate 'subsystem == "riqtu.Hohma" AND category == "Network"'
```

### Поиск ошибок

```bash
# В терминале
log stream --predicate 'subsystem == "riqtu.Hohma" AND (level == "error" OR level == "fault")'

# Или в Console.app используйте фильтр:
# subsystem:riqtu.Hohma level:error
```

### Мониторинг Socket.IO

```bash
log stream --predicate 'subsystem == "riqtu.Hohma" AND category == "SocketIO"'
```

### Просмотр логов авторизации

```bash
log stream --predicate 'subsystem == "riqtu.Hohma" AND category == "Authentication"'
```

## Формат логов

Логи имеют следующий формат:

```
[timestamp] LEVEL: fileName:line function - message [Error: error description]
```

Пример:

```
[2025-12-12 10:30:45] INFO: AuthService.swift:72 login() - ℹ️ User logged in successfully
[2025-12-12 10:30:46] ERROR: NetworkManager.swift:66 request() - ❌ Request failed | Error: Network timeout
```

## Важные особенности

### Debug логи

- **Debug логи видны только в DEBUG режиме**
- В production сборке debug логи не записываются
- Используйте `logDebug()` для отладочной информации

### Безопасность

- Чувствительные данные (токены, пароли) не логируются в production
- Debug логи автоматически отключаются в release сборках

### Производительность

- OSLog оптимизирован для production
- Логи не влияют на производительность приложения
- Debug логи компилируются только в DEBUG режиме

## Полезные команды

### Получить UDID устройства

```bash
# Список всех подключенных устройств
xcrun xctrace list devices

# Или через Instruments
instruments -s devices
```

### Экспорт логов с устройства

```bash
# Экспорт всех логов приложения за последний час
log show --device <UDID> --predicate 'subsystem == "riqtu.Hohma"' --last 1h > logs.txt

# Экспорт только ошибок
log show --device <UDID> --predicate 'subsystem == "riqtu.Hohma" AND (level == "error" OR level == "fault")' --last 24h > errors.txt
```

### Очистка старых логов

```bash
# Очистить все логи приложения (требует sudo)
sudo log collect --last 0s --output /dev/null
```

## Использование в коде

### Базовое использование

```swift
// Debug (только в DEBUG режиме)
AppLogger.shared.debug("Debug message", category: .network)

// Info
AppLogger.shared.info("Info message", category: .general)

// Warning
AppLogger.shared.warning("Warning message", category: .ui)

// Error
AppLogger.shared.error("Error message", error: someError, category: .network)

// Fault (критическая ошибка)
AppLogger.shared.fault("Critical error", error: someError, category: .general)
```

### Глобальные функции (удобные сокращения)

```swift
logDebug("Debug message", category: .network)
logInfo("Info message")
logWarning("Warning message")
logError("Error message", error: someError)
logFault("Critical error", error: someError)
```

### Логирование сетевых запросов

```swift
// Логирование запроса
AppLogger.shared.logRequest(urlRequest, category: .network)

// Логирование ответа
AppLogger.shared.logResponse(httpResponse, data: responseData, error: nil, category: .network)
```

## Troubleshooting

### Логи не видны в терминале Cursor/SweetPad

**Проблема:** Логи не выводятся в терминал при запуске приложения.

**Решения:**

1. **Проверьте, что запускаете DEBUG конфигурацию:**

   - В Xcode: Product → Scheme → Edit Scheme → Run → Build Configuration → Debug
   - В Cursor/SweetPad: убедитесь, что используете Debug схему

2. **Проверьте, что вывод в консоль включен:**

   ```swift
   // В коде приложения (например, в AppDelegate)
   AppLogger.shared.enableConsoleOutput = true
   ```

3. **Убедитесь, что используете правильные методы логирования:**

   ```swift
   // ✅ Правильно - будет видно в терминале
   AppLogger.shared.info("Message")
   logInfo("Message")

   // ❌ Неправильно - не будет видно
   print("Message") // Используйте AppLogger вместо print
   ```

4. **Проверьте, что приложение запущено:**

   - Убедитесь, что приложение действительно запущено и работает
   - Проверьте, что логи вызываются (добавьте тестовый лог в `AppDelegate`)

5. **Если используете симулятор:**

   - Логи должны выводиться в терминал автоматически
   - Если не видно, попробуйте перезапустить приложение

6. **Если используете физическое устройство:**
   - Логи могут не выводиться в терминал напрямую
   - Используйте `log stream` в отдельном терминале (см. раздел выше)

### Логи не отображаются в Console.app

1. Убедитесь, что устройство/симулятор подключен
2. Проверьте, что приложение запущено
3. Используйте правильный subsystem: `riqtu.Hohma`

### Debug логи не видны

- Debug логи работают только в DEBUG режиме
- Убедитесь, что запускаете Debug конфигурацию (не Release)
- Проверьте, что используете `logDebug()` или `AppLogger.shared.debug()`

### Логи не сохраняются на устройстве

- OSLog автоматически управляет хранением логов
- Логи хранятся ограниченное время (обычно несколько дней)
- Для долгосрочного хранения используйте экспорт через `log show`

## Дополнительные ресурсы

- [Apple Documentation: Logging](https://developer.apple.com/documentation/os/logging)
- [WWDC: Unified Logging and Activity Tracing](https://developer.apple.com/videos/play/wwdc2016/721/)
