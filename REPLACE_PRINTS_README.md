# Скрипт замены print() на AppLogger

## Описание

Скрипт `replace_prints.py` автоматически заменяет все `print()` на соответствующие вызовы `AppLogger` в Swift файлах проекта.

## Возможности

- ✅ Автоматическое определение уровня логирования (debug, info, warning, error, fault) по эмодзи и ключевым словам
- ✅ Автоматическое определение категории (.network, .auth, .socket, .cache, .ui, .general) по пути файла
- ✅ Создание резервных копий файлов (.backup)
- ✅ Режим dry-run для предварительного просмотра изменений
- ✅ Обработка одного файла или всей директории

## Использование

### 1. Предварительный просмотр (dry-run)

```bash
# Показать все изменения без применения
python3 replace_prints.py --dry-run

# Показать изменения для конкретного файла
python3 replace_prints.py --dry-run --file Hohma/Features/Home/Views/HomeView.swift
```

### 2. Применение изменений

```bash
# Заменить все print() во всех файлах
python3 replace_prints.py

# Заменить print() в конкретном файле
python3 replace_prints.py --file Hohma/Features/Home/Views/HomeView.swift

# Заменить print() в конкретной директории
python3 replace_prints.py --dir Hohma/Features/Chat
```

## Правила замены

### Уровни логирования

Скрипт определяет уровень логирования по эмодзи и ключевым словам:

- **error**: ❌, "error", "failed", "ошибка", "не удалось"
- **warning**: ⚠️, "warn", "warning", "предупреждение"
- **info**: ✅, 📦, "info", "успешно", "success"
- **debug**: 🔍, 📥, 📤, 🔌, 💬, 🏁, 🎲, 🎮, 🔄, 🔗, 🏠, 📱, ▶️ (по умолчанию)
- **fault**: 💥

### Категории

Категория определяется по пути к файлу:

- `.network` - NetworkManager, TRPCService
- `.auth` - Auth, Authentication
- `.socket` - Socket, SocketIO
- `.cache` - Cache, ImageCache
- `.keychain` - Keychain
- `.ui` - View, ViewModel
- `.general` - остальные файлы

## Примеры

### До:
```swift
print("❌ Ошибка загрузки данных: \(error)")
print("💬 ChatViewModel: Сообщение отправлено")
print("✅ Успешно сохранено")
```

### После:
```swift
AppLogger.shared.error("Ошибка загрузки данных: \(error)", category: .general)
AppLogger.shared.debug("Сообщение отправлено", category: .ui)
AppLogger.shared.info("Успешно сохранено", category: .general)
```

## Резервные копии

Скрипт автоматически создает резервные копии всех измененных файлов с расширением `.backup`.

Для восстановления:
```bash
# Восстановить один файл
mv Hohma/Features/Home/Views/HomeView.swift.backup Hohma/Features/Home/Views/HomeView.swift

# Восстановить все файлы
find Hohma -name "*.backup" -exec sh -c 'mv "$1" "${1%.backup}"' _ {} \;
```

## Ограничения

- Скрипт не обрабатывает многострочные `print()` с переносами строк внутри
- Сложные выражения в `print()` могут требовать ручной доработки
- Файл `AppLogger.swift` пропускается (там `print()` используется для вывода в консоль)

## Рекомендации

1. **Всегда запускайте с `--dry-run` сначала** для проверки изменений
2. **Проверьте изменения** после применения скрипта
3. **Запустите линтер** для проверки синтаксиса: `swiftlint` или в Xcode
4. **Протестируйте приложение** после массовой замены

## Статистика

После запуска скрипт покажет:
- Количество обработанных файлов
- Количество замененных `print()`
- Количество ошибок (если есть)

## Устранение проблем

### Скрипт не находит файлы
Убедитесь, что вы находитесь в корневой директории проекта:
```bash
cd /Users/riqtu/projects/Hohma
python3 replace_prints.py --dry-run
```

### Ошибки при замене
Проверьте синтаксис Swift файлов после замены. Если есть ошибки, восстановите из backup и исправьте вручную.

### Python не найден
Установите Python 3:
```bash
# macOS
brew install python3

# Проверка
python3 --version
```

